{
  "name": "@voxgig/docgen",
  "version": "0.9.2",
  "main": "dist/docgen.js",
  "type": "commonjs",
  "types": "dist/docgen.d.ts",
  "description": "Voxgig Documentation Generator — an sdkgen package supplying documentation targets.",
  "homepage": "https://github.com/voxgig/voxgig-docgen",
  "keywords": [
    "voxgig",
    "docgen",
    "sdkgen-package",
    "openapi",
    "documentation"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/voxgig/docgen.git"
  },
  "scripts": {
    "test": "node --enable-source-maps --test dist-test/**/*.test.js",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test dist-test/**/*.test.js",
    "watch": "tsc --build src test -w",
    "build": "tsc --build src test",
    "clean": "rm -rf node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "embed-version": "node build/version.js",
    "repo-bump": "npm version patch --no-git-tag-version && node -e \"console.log('bumped to v' + require('./package.json').version + ' (package.json only - commit via repo-tag / repo-release)')\"",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-tag-dry": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo \"[dry-run] TAG: v$REPO_VERSION\" && if git rev-parse -q --verify refs/tags/v$REPO_VERSION >/dev/null; then echo \"[dry-run] WARNING: tag v$REPO_VERSION already exists - bump the version first\"; else echo \"[dry-run] would commit -a, push, tag v$REPO_VERSION and push --tags; nothing done\"; fi && git status --short",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-dry": "npm run clean && npm i && npm run repo-publish-quick-dry",
    "repo-publish-quick": "npm run embed-version && npm run build && npm run test && npm run repo-tag && npm stage publish --registry https://registry.npmjs.org --access=public",
    "repo-publish-quick-dry": "npm run build && npm run test && ( npm stage publish --dry-run --registry https://registry.npmjs.org --access=public || echo \"[dry-run] npm stage publish refused (see npm error above) - usually the version is already published; bump package.json before a real release\" )",
    "repo-release": "npm run repo-publish && npm run repo-tag",
    "repo-release-dry": "npm run repo-publish-quick-dry && npm run repo-tag-dry"
  },
  "license": "MIT",
  "files": [
    ".sdk",
    "sdkgen-package.json",
    "model",
    "src",
    "dist",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@hapi/code": "9.0.3",
    "@types/js-yaml": "4.0.9",
    "@types/node": "26.2.0",
    "esbuild": "0.28.2",
    "json-schema-to-ts": "3.1.1",
    "memfs": "4.68.1",
    "typescript": "7.0.2"
  },
  "peerDependencies": {
    "@redocly/openapi-core": ">=2",
    "@voxgig/apidef": ">=7",
    "@voxgig/sdkgen": ">=3.5.0",
    "@voxgig/util": ">=0.5",
    "aontu": ">=0.52",
    "jostraca": ">=0.33",
    "pino": ">=10",
    "pino-pretty": ">=13"
  }
}
